# Fretboard

The fretboard app I needed for myself, but figured other folks might want to mess with it too.

Still shaping it up, still adding bits, still pretending I totally planned all of this.

## Current UI focus

The top control area is intentionally minimal right now and only exposes **Key** and **Scale** selectors while the chord browser/palette workflows are parked.
